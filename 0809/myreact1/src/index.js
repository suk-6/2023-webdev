import React from 'react';
import ReactDOM from 'react-dom/client';
import './static/main.css';
import App from './App-3/App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
